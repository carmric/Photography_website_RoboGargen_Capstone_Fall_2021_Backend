-- This file sets up the connection between the database and the backend of the project. The below lines of code are the connection that fetches the information from the database.
SELECT * FROM photography_website.categories;
SELECT * FROM photography_website.contacts;
SELECT * FROM photography_website.images;